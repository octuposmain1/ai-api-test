[project]
name = "flaskr"
version = "1.0.0"
description = "My own Flask app"
dependencies = [
    "flask",
    "groq",
    "python-dotenv"
]

[tool.flit.module]
name = "flaskr"

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
